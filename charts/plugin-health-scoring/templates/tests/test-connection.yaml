apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plugin-health-scoring.fullname" . }}-test-connection"
  labels:
    {{- include "plugin-health-scoring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plugin-health-scoring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
