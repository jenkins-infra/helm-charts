apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rss2twitter.fullname" . }}-test-connection"
  labels:
    {{- include "rss2twitter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rss2twitter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
