apiVersion: v1
kind: Service
metadata:
  name: {{ include "repo-proxy.fullname" . }}
  labels:
    {{- include "repo-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "repo-proxy.selectorLabels" . | nindent 4 }}
