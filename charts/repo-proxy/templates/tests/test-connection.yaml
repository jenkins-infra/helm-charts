apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "repo-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "repo-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "repo-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
